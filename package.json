{
  "name": "visualize-red-black-tree",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:gh-pages": "tsc && vite build --config build/gh-pages/vite.config.js",
    "build:aws": "tsc && vite build",
    "deploy:gh-pages": "gh-pages -d dist",
    "preview": "vite preview",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.20.2",
    "@babel/preset-typescript": "^7.21.0",
    "@types/jest": "^29.5.0",
    "@types/p5": "^1.5.0",
    "gh-pages": "^5.0.0",
    "jest": "^29.5.0",
    "typescript": "^4.9.3",
    "vite": "^4.2.0"
  },
  "dependencies": {
    "p5": "^1.6.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}